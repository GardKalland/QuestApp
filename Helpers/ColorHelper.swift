import SwiftUI

extension Color {
    static let completedColor = Color("CompletedColor")   // Green for completed tasks
    static let uncompletedColor = Color("UncompletedColor") // Gray for uncompleted tasks
}
